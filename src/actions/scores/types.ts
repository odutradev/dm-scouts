export interface CreateScoreData {
    extraScoreReason?: string;
    observations?: string;
    teamLeaderID: string;
    baseLeaderID: string;
    extraScore?: number;
    outputIn?: Date;
    baseID: string;
    inputIn?: Date;
    score: string;
};